name: reset_flow
description: "A Flutter application to manage daily goals with an advanced dashboard."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  intl: ^0.19.0
  fl_chart: ^0.68.0
  glassmorphism: ^3.0.0
  google_fonts: ^6.3.0
  cupertino_icons: ^1.0.6
  uuid: ^4.3.3
  path: ^1.9.0
  sqflite_common_ffi: ^2.3.3
  flutter_staggered_animations: ^1.1.1
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
